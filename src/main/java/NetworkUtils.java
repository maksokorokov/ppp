public class NetworkUtils {
    public  static void getConnection(){
        // получаем соединение с сервером
        // тестим на скорость подключения к серверу
        try {
            Thread.sleep(1500);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        return;
    }
}
